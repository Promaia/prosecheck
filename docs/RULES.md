# ADRs are immutable after acceptance

Accepted ADRs in `docs/adr/` should not have their Decision or Context sections modified. If a decision is superseded, create a new ADR that references the old one.
