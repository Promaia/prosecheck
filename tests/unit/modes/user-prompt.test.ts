import { describe, it, expect } from 'vitest';

describe('user-prompt mode', () => {
  it.todo('builds orchestration prompt listing all prompt files');
  it.todo('watches output directory for result files');
});
