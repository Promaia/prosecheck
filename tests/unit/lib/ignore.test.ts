import { describe, it, expect } from 'vitest';

describe('ignore', () => {
  it.todo('matches gitignore-formatted patterns');
  it.todo('combines globalIgnore with additionalIgnore file patterns');
  it.todo('handles missing additionalIgnore files gracefully');
  it.todo('evaluates per-rule inclusion patterns');
});
