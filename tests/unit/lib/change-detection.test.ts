import { describe, it, expect } from 'vitest';

describe('change-detection', () => {
  it.todo('detects changed files from git diff');
  it.todo('maps changed files to rule scopes');
  it.todo('applies global ignore filtering');
  it.todo('uses last-run hash when enabled');
});
