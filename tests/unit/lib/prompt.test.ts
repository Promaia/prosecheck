import { describe, it, expect } from 'vitest';

describe('prompt', () => {
  it.todo('generates per-rule prompt with default template');
  it.todo('uses custom prompt-template.md when present');
  it.todo('prepends global prompt.md when present');
  it.todo('includes changed files list in prompt');
});
