import { describe, it, expect } from 'vitest';

describe('config', () => {
  it.todo('loads config from .prosecheck/config.json');
  it.todo('merges config.local.json on top of config.json');
  it.todo('applies environment overrides');
  it.todo('CLI flags override everything');
});
