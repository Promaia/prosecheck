import { describe, it, expect } from 'vitest';

describe('adr calculator', () => {
  it.todo('reads ADR files from configured path');
  it.todo('derives rules from ADR decisions');
  it.todo('applies project-wide inclusion by default');
});
