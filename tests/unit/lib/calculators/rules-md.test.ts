import { describe, it, expect } from 'vitest';

describe('rules-md calculator', () => {
  it.todo('discovers RULES.md files in project tree');
  it.todo('parses headings as rule names');
  it.todo('includes subheadings in rule description');
  it.todo('ignores text before first heading');
  it.todo('sets inclusion to containing directory');
});
