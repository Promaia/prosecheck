import { describe, it, expect } from 'vitest';

describe('init command', () => {
  it.todo('scaffolds .prosecheck/ directory');
  it.todo('creates config.json with defaults');
  it.todo('adds .prosecheck entries to .gitignore');
});
